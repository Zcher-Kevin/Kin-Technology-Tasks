Here are the three tasks that I have completed.\\
which are :\\
2. **Asynchronous_API_Fetch**\\
Write an asynchronous function that fetches data from the API at https://jsonplaceholder.typicode.com/posts and logs the titles of the first five posts.

Here is the command to run the task.
```shell
cd Asynchronous_API_Fetch
node API_Fetch.js
```

3. **React Counter Component**\\
Create a simple React component that renders a button and a counter. Clicking the button should increment the counter by 1.

Here is the command to run the task.
```shell
cd react_counter_component
npm run dev
```

5. **Responsive Navigation Bar**\\
Create a responsive navigation bar using only HTML and CSS. The navbar should:
- Include links for “Home,” “About,” and “Contact.”
- Collapse into a dropdown menu on screens smaller than 768px.

